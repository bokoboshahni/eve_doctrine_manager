<!DOCTYPE html>
<html lang="en" class="h-full bg-neutral-100">
  <head>
    <title><%= content_for?(:title) ? "#{content_for(:title)} – EVE Doctrine Manager" : 'EVE Doctrine Manager' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500&family=Source+Code+Pro:ital,wght@0,400;1,300;1,400&display=swap" rel="stylesheet">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <div class="bg-neutral-800 pb-32">
        <%= render 'application/top_nav' %>
        <%= render 'application/header' %>
      </div>

      <main class="-mt-32">
        <div class="mx-auto max-w-7xl px-4 pb-12 sm:px-6 lg:px-8">
          <!-- Replace with your content -->
          <div class="rounded-lg bg-white px-5 py-6 shadow sm:px-6">
            <div class="h-96 rounded-lg border-4 border-dashed border-neutral-200"></div>
          </div>
          <!-- /End replace -->
        </div>
      </main>
    </div>
  </body>
</html>
